const N = "PROFILE_MANAGER_";

window.$$cmds = {
  SetState: `${N}SET_STATE`,
  GetState: `${N}GET_STATE`,
  CheckSecret: `${N}CHECK_SECRET`,
  SaveState: `${N}SAVE_STATE`,
  LoadState: `${N}LOAD_STATE`,
};
